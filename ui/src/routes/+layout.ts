// TST Desk is a client-side SPA inside the Tauri webview — there is no server
// runtime (AGENTS.md §1). Prerendered static shell + fallback for client routes.
export const ssr = false;
export const prerender = true;
