// TST Desk is a client-side SPA (Tauri webview or a browser attach, TD-3701)
// — there is no server runtime (AGENTS.md §1). Prerendered static shell +
// fallback for client routes.
export const ssr = false;
export const prerender = true;
