# Gotchas

`GOTCHA: The context assembler (E5) is the critical path for both Steering and Memory. If the heading-match loader (TD-2201) fails to correctly parse topic headers, the agent will . . . breaking the "Brain" capability before it even starts.`
